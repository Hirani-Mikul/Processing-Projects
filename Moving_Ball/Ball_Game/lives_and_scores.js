var lives = 3;
var scores = 0;
function Lives () {
  textSize(25);
  fill(150, 0, 200);
  text ("Lives: " + lives, 500, 25);
}
function Scores () {
  textSize(25);
  fill(150, 0, 200);
  text ("Scores: " + scores, 10, 25);
}
